---
import InputField from '../ui/InputField.astro';
import SelectField from '../ui/SelectField.astro';
import CountrySelect from '../ui/CountrySelect.astro';
import Button from '../ui/Button.astro';
import PhoneInput from '../ui/PhoneInput.astro';
import FormError from '../ui/FormError.astro';
import FormSubmitButton from '../ui/FormSubmitButton.astro';
import FormSuccess from '../ui/FormSuccess.astro';
import { ROUTES } from '../../config/routes';
---

<div id="form-matricula-wrapper">
  <form id="matricula-form" method="POST" class="space-y-8" novalidate>
    <div class="flex items-center justify-between mb-12 relative px-2">
      <div class="absolute left-0 top-1/2 -translate-y-1/2 w-full h-1 bg-border-subtle rounded-full z-0"></div>
      <div id="progress-bar" class="absolute left-0 top-1/2 -translate-y-1/2 h-1 bg-imb rounded-full z-0 transition-all duration-500" style="width: 0%"></div>
      {[1, 2, 3, 4].map((s) => (
        <div id={`step-circle-${s}`} class={`relative z-10 w-10 h-10 rounded-full flex items-center justify-center font-display font-bold text-sm transition-all duration-300 border-2 ${s === 1 ? 'bg-imb text-surface-0 border-imb shadow-md' : 'bg-surface-0 text-icon-muted border-border-subtle'}`}>
          <span class="step-number">{s}</span>
          <svg class="step-check hidden w-5 h-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
        </div>
      ))}
    </div>

    <div id="step-1" class="step-content space-y-8">
      <h3 class="text-component-title">Curso</h3>
      <InputField label="Escribe el curso al que te matriculas" name="curso" required />
    </div>

    <div id="step-2" class="step-content space-y-8 hidden">
      <h3 class="text-component-title">Datos personales</h3>
      <InputField label="Nombres y apellidos completos" name="nombres" required placeholder="María Torres" />
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <InputField class="md:[&>label]:min-h-10" label="Número de documento, cédula o carnet de identidad" name="documento" required maxLength={25} help="Máximo 25 caracteres." />
        <InputField class="md:[&>label]:min-h-10" label="Correo electrónico" name="correo" type="email" required placeholder="maria.torres@correo.com" />
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <PhoneInput label="Número de WhatsApp" name="whatsapp" id="matricula-whatsapp" required />
        <CountrySelect label="País" name="pais" required placeholder="Selecciona un país" />
      </div>
    </div>

    <div id="step-3" class="step-content space-y-8 hidden">
      <h3 class="text-component-title">Datos profesionales y tecnología</h3>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <InputField label="Área de formación o profesión" name="formacion" required={false} />
        <InputField label="Empresa donde trabajas actualmente (o última)" name="empresa" required />
      </div>
      <InputField label="Cargo u ocupación" name="cargo" required placeholder="Jefe de planta" />
      <SelectField
        label="Nivel de familiaridad con el uso de tecnología"
        name="familiaridad_tecnologica"
        required
        options={[
          { value: '', label: 'Selecciona tu nivel de familiaridad' },
          'No tengo experiencia previa',
          'Básico (uso limitado, con apoyo)',
          'Intermedio (uso habitual de herramientas digitales)',
          'Avanzado (uso frecuente y autónomo)',
        ]}
      />
    </div>

    <div id="step-4" class="step-content space-y-8 hidden">
      <h3 class="text-component-title">Facturación, beneficios adicionales y términos</h3>
      <SelectField
        label="Tipo de comprobante de pago"
        name="tipo_comprobante"
        required
        options={[
          { value: '', label: 'Selecciona el tipo de comprobante' },
          'Boleta',
          'Factura',
          'Recibo',
        ]}
      />
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <InputField label="Razón social o nombre completo para registrar la facturación" name="razon_social" required maxLength={25} help="Máximo 25 caracteres." />
        <InputField label="Número de identificación tributaria" name="numero_tributario" required maxLength={25} help="RUC, RUT, RFC, NIT u otro. Máximo 25 caracteres." />
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <SelectField
          label="Clases de inglés"
          name="nivel_ingles"
          options={[
            { value: '', label: 'Selecciona tu nivel' },
            'Ninguno',
            'Básico',
            'Intermedio',
            'Avanzado',
          ]}
        />
        <SelectField
          label="Clases de Excel"
          name="nivel_excel"
          options={[
            { value: '', label: 'Selecciona tu nivel' },
            'Ninguno',
            'Básico',
            'Intermedio',
            'Avanzado',
          ]}
        />
      </div>

      <div class="flex items-start gap-3">
        <input
          id="matricula-terminos"
          type="checkbox"
          name="terminos"
          required
          class="mt-1 size-4 shrink-0 cursor-pointer rounded border-border-subtle bg-surface-50 text-imb focus:ring-2 focus:ring-imb/20 focus:ring-offset-2"
        />
        <div>
          <label for="matricula-terminos" class="cursor-pointer font-sans text-sm leading-relaxed text-text-secondary">
            Acepto los <a href={ROUTES.TERMINOS_CONDICIONES_USO} target="_blank" rel="noopener noreferrer" class="font-semibold text-imb underline decoration-imb/40 underline-offset-3 transition-colors hover:text-imb-hover focus-visible:rounded-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-imb">Términos y Condiciones de Uso</a>.
          </label>
          <span class="mt-1 hidden text-xs font-medium text-feedback-error" data-error-for="terminos"></span>
        </div>
      </div>
    </div>

    <FormError id="form-matricula-error" messageId="form-matricula-error-msg" />

    <div class="mt-12 flex justify-between items-center pt-8">
      <div id="btn-back-wrapper" class="hidden">
        <Button type="button" id="btn-back" variant="outline">Regresar</Button>
      </div>
      <div class="grow"></div>
      <div id="btn-next-wrapper">
        <Button type="button" id="btn-next" class="px-10 py-4">Continuar</Button>
      </div>
      <div id="btn-submit-wrapper" class="hidden">
        <FormSubmitButton id="form-matricula-btn" label="Finalizar matrícula" fullWidth={false} class="px-10" />
      </div>
    </div>
  </form>

  <FormSuccess id="form-matricula-success" title="Ficha enviada">
    <p>
      Hemos recibido tu ficha de matrícula. Un asesor académico se comunicará contigo para confirmar los próximos pasos.
    </p>
  </FormSuccess>
</div>

<script>
  import { actions, isInputError } from 'astro:actions';

  function initMatricula() {
    const form = document.getElementById('matricula-form') as HTMLFormElement;
    if (!form || form.dataset.initialized) return;
    form.dataset.initialized = 'true';

    const totalSteps = 4;
    let currentStep = 1;

    const stepContents = document.querySelectorAll('.step-content');
    const circles = [1, 2, 3, 4].map((i) => document.getElementById(`step-circle-${i}`));
    const progressBar = document.getElementById('progress-bar');
    const btnBack = document.getElementById('btn-back');
    const btnNext = document.getElementById('btn-next');
    const btnBackWrapper = document.getElementById('btn-back-wrapper');
    const btnNextWrapper = document.getElementById('btn-next-wrapper');
    const btnSubmitWrapper = document.getElementById('btn-submit-wrapper');
    
    const errorEl = document.getElementById('form-matricula-error');
    const errorMsg = document.getElementById('form-matricula-error-msg');
    const successEl = document.getElementById('form-matricula-success');
    const btnSubmit = document.getElementById('form-matricula-btn') as HTMLButtonElement;
    const btnText = btnSubmit?.querySelector('.btn-text');
    const btnSpinner = btnSubmit?.querySelector('.btn-spinner');

    function updateWizard() {
      stepContents.forEach((el, i) => { el.classList.toggle('hidden', i + 1 !== currentStep); });
      if (progressBar) progressBar.style.width = `${((currentStep - 1) / (totalSteps - 1)) * 100}%`;
      circles.forEach((circle, i) => {
        const stepNum = i + 1;
        const numEl = circle?.querySelector('.step-number');
        const checkEl = circle?.querySelector('.step-check');
        if (stepNum < currentStep) {
          circle?.classList.remove('bg-surface-0', 'text-icon-muted', 'border-border-subtle');
          circle?.classList.add('bg-imb', 'text-surface-0', 'border-imb', 'shadow-md');
          numEl?.classList.add('hidden'); checkEl?.classList.remove('hidden');
        } else if (stepNum === currentStep) {
          circle?.classList.remove('bg-surface-0', 'text-icon-muted', 'border-border-subtle');
          circle?.classList.add('bg-imb', 'text-surface-0', 'border-imb', 'shadow-md');
          numEl?.classList.remove('hidden'); checkEl?.classList.add('hidden');
        } else {
          circle?.classList.remove('bg-imb', 'text-surface-0', 'border-imb', 'shadow-md');
          circle?.classList.add('bg-surface-0', 'text-icon-muted', 'border-border-subtle');
          numEl?.classList.remove('hidden'); checkEl?.classList.add('hidden');
        }
      });

      // Back button visibility
      if (currentStep === 1) {
        btnBackWrapper?.classList.add('hidden');
      } else {
        btnBackWrapper?.classList.remove('hidden');
      }

      // Next / Submit button visibility
      if (currentStep === totalSteps) {
        btnNextWrapper?.classList.add('hidden');
        btnSubmitWrapper?.classList.remove('hidden');
      } else {
        btnNextWrapper?.classList.remove('hidden');
        btnSubmitWrapper?.classList.add('hidden');
      }

      // Hide general error when navigating steps
      errorEl?.classList.add('hidden');
      errorEl?.classList.remove('flex');
    }

    function validateCurrentStep(): boolean {
      const currentStepEl = document.getElementById(`step-${currentStep}`);
      if (!currentStepEl) return true;

      type ValidatableControl = {
        checkValidity: () => boolean;
        reportValidity: () => boolean;
      };
      const inputs = Array.from(
        currentStepEl.querySelectorAll('input, select, textarea')
      ) as unknown as ValidatableControl[];
      const firstInvalid = inputs.find((input) => !input.checkValidity());

      if (firstInvalid) {
        firstInvalid.reportValidity();
        return false;
      }
      return true;
    }

    btnNext?.addEventListener('click', () => {
      if (!validateCurrentStep()) return;
      if (currentStep < totalSteps) { currentStep++; updateWizard(); }
    });

    btnBack?.addEventListener('click', () => {
      if (currentStep > 1) { currentStep--; updateWizard(); }
    });

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      // Clear previous errors
      errorEl?.classList.add('hidden');
      errorEl?.classList.remove('flex');
      form.querySelectorAll('[data-error-for]').forEach(el => {
        el.textContent = '';
        el.classList.add('hidden');
      });

      btnSubmit.disabled = true;
      if (btnText) btnText.textContent = 'Enviando...';
      btnSpinner?.classList.remove('hidden');

      try {
        const formData = new FormData(form);
        const { error } = await actions.matricula(formData);

        if (error) {
          if (isInputError(error)) {
            // Map of field names to their step numbers
            const fieldStepMap: Record<string, number> = {
              curso: 1,
              nombres: 2, documento: 2, pais: 2, correo: 2, whatsapp: 2,
              formacion: 3, empresa: 3, cargo: 3, familiaridad_tecnologica: 3,
              tipo_comprobante: 4, razon_social: 4, numero_tributario: 4, nivel_ingles: 4, nivel_excel: 4, terminos: 4,
            };

            let firstErrorStep = totalSteps;

            for (const [field, messages] of Object.entries(error.fields)) {
              const spanError = form.querySelector(`[data-error-for="${field}"]`);
              if (spanError && messages) {
                spanError.textContent = messages[0];
                spanError.classList.remove('hidden');
              }
              const step = fieldStepMap[field];
              if (step && step < firstErrorStep) firstErrorStep = step;
            }

            // Navigate to the step containing the first error
            if (firstErrorStep !== currentStep) {
              currentStep = firstErrorStep;
              updateWizard();
            }

            // Scroll to the first visible error
            const firstErrorSpan = form.querySelector('[data-error-for]:not(.hidden)');
            if (firstErrorSpan) {
              const fieldEl = firstErrorSpan.parentElement || firstErrorSpan;
              fieldEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
            }
            return;
          }
          throw new Error(error.message || 'Ocurrió un error al enviar el formulario.');
        }

        form.classList.add('hidden');
        successEl?.classList.remove('hidden');
        successEl?.classList.add('flex');
      } catch (err: any) {
        if (errorMsg) errorMsg.textContent = err.message || 'Error de conexión. Intente de nuevo.';
        errorEl?.classList.remove('hidden');
        errorEl?.classList.add('flex');
      } finally {
        btnSubmit.disabled = false;
        if (btnText) btnText.textContent = 'Finalizar matrícula';
        btnSpinner?.classList.add('hidden');
      }
    });

    // Clear field errors on input (consistent with other forms)
    form.addEventListener('input', (e) => {
      const target = e.target as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement;
      if (target.name) {
        const errorSpan = form.querySelector(`[data-error-for="${target.name}"]`);
        if (errorSpan && !errorSpan.classList.contains('hidden')) {
          errorSpan.classList.add('hidden');
        }
      }
    });

    updateWizard();
  }

  document.addEventListener('astro:page-load', initMatricula);
  initMatricula();
</script>
